Match the art style, shading, camera angle and shapes of the attached pot sheet exactly — the same 10 containers, same sizes, same lighting, same soil surface.

The only difference: render everything in PURE GRAYSCALE. Zero saturation, no color tint of any kind, not even a warm or cool cast. Only white, grays and black. R must equal G must equal B for every pixel.

Keep every highlight and shadow exactly as in the color version. The gray values must carry the same 4:1 light-to-dark contrast, because these sprites get recolored in the app by multiplying a color over them — flat gray would produce flat plastic-looking pots.

The soil surface inside stays dark gray, and the glass jar stays a very light gray.

Style: isometric 2:1 dimetric projection, camera elevated about 30 degrees, viewed from the front-upper-left. Flat vector illustration with soft cel shading, clean crisp edges, no outlines. Single light source from the upper left; one soft contact shadow directly beneath each object.

Layout: arranged on an evenly spaced grid, each item fully separated with at least 60 pixels of empty space around it.

Background: fully transparent PNG with alpha. No background color, no gradient, no ground plane, no surface, no table. No text, no labels, no numbers, no watermark.

Output: 1536x1024 PNG with transparency.
